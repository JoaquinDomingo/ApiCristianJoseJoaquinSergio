ktor {
  application {
    modules = [com.ktor.MyApplicationKt.myModule]
  }
  deployment {
    port = 8081
    host = "0.0.0.0"
  }
}

database {
  url = "jdbc:mariadb://db:3306/dbConsole"
  driver = "org.mariadb.jdbc.Driver"
  user = "admin"
  password = "admin"
  maxPoolSize = 10
}